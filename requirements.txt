# Firebase Admin SDK
firebase-admin>=6.2.0

# Flask Web Framework
flask>=2.3.0

# Environment variables
python-dotenv>=1.0.0

# For handling dates and JSON
python-dateutil>=2.8.0

# Note: SQLite3 is included in Python standard library (built-in)
# No need to install mysql-connector-python anymore!

# TTL cache — thread-safe, no Redis dependency
cachetools>=5.3.0

# WSGI web server for production
gunicorn>=21.0.0
